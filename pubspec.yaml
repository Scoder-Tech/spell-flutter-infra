name: flutter_infra_scoder
description: A package for infrastructure to use in flutter projects
version: 0.0.1
publish_to: "none"
homepage:

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.3.3
  pretty_dio_logger: ^1.3.1
  logger: ^2.0.2+1
  localization: ^2.1.0
  flutter_i18n: ^0.35.0
  intl: ^0.19.0
  money2: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
